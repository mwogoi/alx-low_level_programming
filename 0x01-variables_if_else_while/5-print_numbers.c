#include <stdio.h>
/**
 * main - Entry point
 * Description: Print single digit numbers below 10
 * Return: Always 0
 */
int main(void)
{
	int numb;

	for (a = 0; a < 10; a++)
	{
		printf("%i", a);
	}
	putchar('\n');
	return (0);
}
