#include "main.h"
/**
 * _abs - Checks the absolute value of the interger
 * @numb: The numbers being tested
 * Return: The absolute number  value
 */
int _abs(int numb)
{
	if (numb >= 0)
		return (numb);
	else
		return (-numb);
}
